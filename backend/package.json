{
  "name": "backend",
  "version": "0.0.0",
  "scripts": {
    "dev": "ts-node-dev --ignore-watch star-wars-typegen.ts --no-notify --transpile-only --respawn ./src",
    "codegen": "ts-node --log-error src/schema"
  },
  "dependencies": {
    "apollo-server": "^3.10.2",
    "graphql": "^16.6.0",
    "nexus": "^1.3.0"
  },
  "devDependencies": {
    "@types/node": "^18.7.18",
    "ts-node": "^10.9.1",
    "ts-node-dev": "^2.0.0",
    "typescript": "^4.8.3"
  }
}
